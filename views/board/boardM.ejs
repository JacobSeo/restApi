<!DOCTYPE html>
<html>
  <head>
    <%- include('../partials/head') %>
  </head>
  <body>
    <%- include('../partials/nav') %>
    <div class="container mb-3">
      <h2 class="mb-3">Board</h2>
      <table class="board-table table table-sm border-bottom">
        <thead class="thead-light">
          <tr>
            <th scope="col" class="col-sm-2">no</th>
            <th scope="col" class="col-sm-5">Title</th>
            <th scope="col" class="col-sm-2">등록자</th>
            <th scope="col" class="col-sm-3 date">Date</th>
          </tr>
        </thead>
        <tbody>
          <% if(data == null || data.length == 0){ %>
            <tr>
              <td colspan=3>리스트 없음</td>
            </tr>
          <% } %>
          <% data.forEach(function(val) { %>
            <tr>
              <td><span><%=val.seq%></span></td>
              <td>
                <a href="/board/<%= val.seq %>"><div class="ellipsis"><%= val.title %></div></a>
              </td>              
              <td><%= val.ins_nm %></a></td>
              <td class="date">
                <span data-date=""><%= val.ins_dt %></span> <!-- 1 -->
              </td>
            </tr>
          <% }) %>
        </tbody>
      </table>
      <div>
        <a class="btn btn-primary" href="/board/new">New</a>
      </div>
    </div>
  </body>
</html>